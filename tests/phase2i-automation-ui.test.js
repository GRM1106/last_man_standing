import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("Phase 2I operations UI",()=>{const admin=readFileSync(new URL("../admin.js",import.meta.url),"utf8");it("shows state, last run, review block and shared run-now control",()=>{expect(admin).toContain("Run automation now");expect(admin).toContain("Blocked by review");expect(admin).toContain("Last run:");expect(admin).toContain('supabase.rpc("run_lms_pot_automation"');});});
